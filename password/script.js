const correctPassword = "secure123";const passwordPromptElement = document.getElementById("password-prompt");const protectedContentElement = document.getElementById("protected-content");const passwordInputElement = document.getElementById("password-input");const submitButtonElement = document.getElementById("submit-button");const errorMessageElement = document.getElementById("error-message");const correctPasswordStored = correctPassword;const passwordPrompt = passwordPromptElement;const protectedContent = protectedContentElement;const passwordInput = passwordInputElement;const submitButton = submitButtonElement;const errorMessage = errorMessageElement;let passwordInputValue = "";let enteredPassword = "";submitButton.addEventListener("click", function () {    passwordInputValue = passwordInput.value;    enteredPassword = passwordInputValue;    if (enteredPassword === correctPasswordStored) {        passwordPrompt.style.display = "none";        protectedContent.style.display = "block";    } else {        errorMessage.style.display = "block";    }});const hidePasswordPrompt = function() {    passwordPrompt.style.display = "none";};const showProtectedContent = function() {    protectedContent.style.display = "block";};const showErrorMessage = function() {    errorMessage.style.display = "block";};const handlePasswordVerification = function(enteredPassword) {    if (enteredPassword === correctPasswordStored) {        hidePasswordPrompt();        showProtectedContent();    } else {        showErrorMessage();    }};submitButtonElement.addEventListener("click", function () {    passwordInputValue = passwordInputElement.value;    enteredPassword = passwordInputValue;    handlePasswordVerification(enteredPassword);});
